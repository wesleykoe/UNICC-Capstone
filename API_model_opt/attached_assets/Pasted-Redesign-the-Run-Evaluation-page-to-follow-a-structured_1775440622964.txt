Redesign the "Run Evaluation" page to follow a structured 3-step workflow aligned with the backend evaluation schema.

GOALS:
- Make the UI intuitive for non-technical UN users
- Replace free-text inputs with structured fields
- Align input with the evaluation schema (ai_system, deployment_context, evaluation_scenarios)
- Reduce cognitive load and improve clarity

CHANGES:

1. STEP 1 — AI SYSTEM INFORMATION
Replace "AI Agent" section with structured inputs:
- System Name (text)
- Version (text)
- Purpose (textarea)
- Declared Constraints (multi-input tags)

2. STEP 2 — DEPLOYMENT CONTEXT
Add a new section:
- Organization Type (dropdown: UN Agency, NGO, Government, Other)
- User Type (text)
- Risk Tolerance Level (dropdown: Low, Moderate, High)
- Geographic Scope (text)

3. STEP 3 — SCENARIO BUILDER
Replace current textarea with structured fields:
- Scenario Type (dropdown: Normal, Edge, Adversarial, Standard)
- Risk Category (dropdown: Governance, Security, Behavioral, Shared)
- Input Prompt (textarea)
- Expected Behavior (textarea)

4. QUICK TEMPLATES (UPGRADE)
Replace current buttons with:
- Political Pressure Test (Governance)
- Data Leakage Test (Threat)
- Harmful Output Test (Behavioral)

Each template should auto-fill ALL scenario fields.

5. REMOVE EXPERT SELECTION
Delete "Expert Modules" section entirely.
System always runs all experts.

6. RUN BUTTON UX
Change button to:
"🚀 Run Safety Evaluation"

Add loading state:
- Spinner
- Text: "Running expert analysis (~30–60s)..."

7. PRE-RUN INFO PANEL
Add a small info box above button:
"You will receive:
• Expert risk assessments
• Final council decision
• Required mitigation actions"

8. VISUAL IMPROVEMENTS
- Use cards for each step
- Add step indicators (Step 1 / Step 2 / Step 3)
- Increase spacing and hierarchy
- Keep consistent style with Dashboard and Council Decision pages

IMPORTANT:
Do NOT change backend API structure.
Only improve frontend UX and input structure.